export enum LAUNCH_MODE_ENUM {
  automator = 'automator',
  manager = 'manager',
}

export enum MANAGE_ACCOUNT_ENUM {
  add = 'add',
  delete = 'delete',
  change_name = 'change_name',
  change_proxy = 'change_proxy',
}

export enum FLAG_ENUM {
  au = '--automator',
  ma = '--manager',
}
